import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        gap: 15
    },
    title: {
        fontSize: 20,
        fontWeight: '600'
    },
    tasks: {
        gap: 15
    },
    tasksContainer: {
        marginBottom: 380
    }
})