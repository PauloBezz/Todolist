import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    circle: {
        height: 10,
        width: 10,
        position: 'absolute',
        backgroundColor: '#6F51FF',
        borderRadius: 10,
        top: -5,
        right: -5
    }
})